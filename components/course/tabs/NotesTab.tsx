interface Props { selectedChapterIndex: number; }
export default function NotesTab({ selectedChapterIndex: _ }: Props) {
  return <div style={{ padding: 32, color: 'var(--uniflow-text-2)' }}>Notes coming soon.</div>;
}
